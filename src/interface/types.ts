export interface Skip {
  id: string;
  name: string;
  size: string;
  description: string;
  price: number;
  image: string;
  dimensions: string;
  capacity: string;
  popular?: boolean;
  features: string[];
}